android {
    namespace "com.premiertransit.app"
    compileSdkVersion 34

    defaultConfig {
        applicationId "com.premiertransit.app"
        minSdkVersion 21
        targetSdkVersion 34
        versionCode 1
        versionName "1.0.0"
    }

    buildTypes {
        release {
            signingConfig signingConfigs.debug
        }
    }
}

flutter {
    source '../..'
}